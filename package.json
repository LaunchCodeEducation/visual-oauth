{
  "name": "visual-oauth",
  "version": "0.0.1",
  "description": "A tool for visualizing the most common (authorization code grant) OAuth flow in a step-by-step interactive process",
  "author": "the-vampiire",
  "license": "GPL-3.0",
  "scripts": {
    "setup": "bash setup.sh",
    "install:api": "cd src/api && npm install",
    "install:client": "cd src/client && npm install",
    "install": "npm run install:api && npm run install:client",
    "start:api": "cd src/api && npm run dev",
    "start:client": "cd src/client && npm start",
    "start": "npm run start:api & npm run start:client"
  }
}
