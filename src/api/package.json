{
  "name": "visual-oauth-api",
  "version": "0.0.1",
  "description": "Back-end API for Visual OAUth tool",
  "author": "the-vampiire",
  "license": "GPL-3.0",
  "main": "src",
  "environment": {
    "CLIENT_ORIGIN": "hosted / local client origin",
    "GITHUB_CLIENT_ID": "client ID for registered github oauth app",
    "GITHUB_CLIENT_SECRET": "client secret for registered github oauth app",
    "GITHUB_REDIRECT_URI": "client redirect domain path",
    "GITHUB_ACCESS_TOKEN_ENDPOINT": "GitHub API OAuth access token endpoint"
  },
  "scripts": {
    "test": "npm test",
    "env:dev": "env $(< .env)",
    "dev": "npm run env:dev -- nodemon ."
  },
  "dependencies": {
    "axios": "^0.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
