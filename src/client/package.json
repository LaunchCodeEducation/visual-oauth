{
  "name": "client",
  "version": "0.0.1",
  "description": "front-end client for Visual Oauth tool",
  "author": "the-vampiire",
  "license": "GPL-3.0",
  "environment": {
    "REACT_APP_API_DOMAIN": "hosted / local API domain address",
    "REACT_APP_GITHUB_AUTH_ENDPOINT": "GitHub OAuth authorization endpoint (must include client_id, redirect_uri and scope qs params)"
  },
  "dependencies": {
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "react-scripts": "^3.4.1",
    "react-syntax-highlighter": "^11.0.2",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^0.88.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
